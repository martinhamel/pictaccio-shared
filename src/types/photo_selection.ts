export type PhotoSelection = {
    itemType: 'subject' | 'group';
    backgroundProductionIdentifier?: string;
    backgroundUrl?: string;
    photoId: string;
    photoUrl: string;
    itemId: string;
}
